# Visual assets

ヒーロー背景用の動画・画像をここに置きます。以下の名前で配置するとそのまま参照されます:

- `hero.webm` (推奨 / 高品質) — 背景ループ動画
- `hero.mp4` (フォールバック) — webm を再生できないブラウザ向け
- `hero-poster.jpg` (ポスター) — 動画読み込み前の静止画

ファイルは `/visuals/<name>` で配信されます。解像度は 1080p 以下推奨、ビットレートは抑えて初期ロードを軽くしてください。ループ用なら 10〜20 秒程度で十分です。
